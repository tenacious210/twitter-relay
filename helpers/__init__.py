# use __init__.py files to denote a folder as a python module
# this module should helpers for reading/writing to files, reading data from websites, etc.

from .config import logging, DISCORD_TOKEN, read_channels, write_channels
